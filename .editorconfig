# Whatever your editor is, start from the same place `cargo fmt` would.
root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true
indent_style = space
indent_size = 4
max_line_length = 100

[*.{md,markdown}]
# Two trailing spaces are a hard line break in Markdown; trimming them silently
# changes the rendering.
trim_trailing_whitespace = false

[*.{toml,yml,yaml,json}]
indent_size = 2

[Makefile]
indent_style = tab

[*.rb]
indent_size = 2
